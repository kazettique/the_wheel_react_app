import React from 'react'

// UNDER CONSTRUCTION... NOT DONE YET!!
const ProgressBar = props => {
  return (
    <>
      <ProgressBar
        // variant="danger"
        now={this.state.c_fundNow / this.state.c_fundGoal}
        label={this.state.c_fundNow / this.state.c_fundGoal}
        // style={{background: '#f52a2a'}}
        // style={{backgroundColor: '#cccccc !important !important'}}
      />
    </>
  )
}

export default ProgressBar
